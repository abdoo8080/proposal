\begin{figure}[ht!]
    \centering
    \includegraphics[scale=0.8]{images/sygus-solver.pdf}
    \label{fig:sygus-solver}
    \caption{\begin{small}
            %
            The diagram shows the SyGuS solver at work.
            %
            Using the grammar (1), the enumerator produces a term (2) and sends it to the SMT solver.
            %
            The solver checks the term against the constraints (3) and if satisfied, outputs the program (5).
            %
            Otherwise, asks the enumerator for the next term (4).
        \end{small}}
\end{figure}
