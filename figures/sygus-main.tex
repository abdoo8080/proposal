\begin{figure}[ht!]
    \centering
    \includegraphics[scale=0.8]{images/sygus-solver.pdf}
    \label{fig:sygus-solver}
    \caption{\begin{small}
            %
            The diagram shows the SyGuS solver at work.
            %
            Using the grammar (1), the program generator produces a candidate solution (3) and sends it to the Program verifier.
            %
            The verifier checks the candidate program against the constraints (2).
            %
            If they are satisfied (5), the solver outputs the program (6).
            %
            Otherwise, the solver asks the generator for the next program (4).
        \end{small}}
\end{figure}
